<Window x:Class="Частный_детектив.DetectiveSmses"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Частный_детектив"
        mc:Ignorable="d"
        Title="Клиенты" Height="600" Width="600" Closed="Window_Closed" Loaded="Window_Loaded">
    <Window.Resources>
        <Style x:Key="People" TargetType="Button">
            <Setter Property="Height" Value="50" />
            <Setter Property="Margin" Value="20" />
            <Setter Property="Background" Value="CornflowerBlue"/>
            <Setter Property="Foreground" Value="White"/>
        </Style>
    </Window.Resources>
    <Grid>
        <ScrollViewer>
            <StackPanel x:Name="stack">
                <Border Margin ="15,15,280,15" BorderBrush="#FF303FD3" BorderThickness="2">
                    <TextBlock Cursor="Hand" x:Name="up" TextWrapping="Wrap" Grid.Column="0" FontSize="20" ><Run Foreground="#FF303FD3" Text="Агентство" MouseLeftButtonDown="TextBlock_MouseLeftButtonDown"/><LineBreak/><Run FontSize="24" Text="Частный детектив"/></TextBlock>
                </Border>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</Window>

